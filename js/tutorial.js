sesion.requerir(["trabajador"]);const lista=document.getElementById("tutorial-lista");function escapar(v){return String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]));}function cargar(){const items=JSON.parse(localStorage.getItem("cicsa_tutoriales")||"[]");lista.innerHTML=items.length?items.map(t=>`<article class="tutorial-item"><h2>${escapar(t.titulo)}</h2><p>${escapar(t.descripcion||"")}</p>${t.tipo==="video"?`<video controls preload="metadata" src="${escapar(t.url)}"></video>`:t.tipo==="pdf"?`<a class="btn btn-primario" target="_blank" rel="noopener" href="${escapar(t.url)}">Abrir documento PDF</a>`:`<a href="${escapar(t.url)}" target="_blank" rel="noopener">${escapar(t.url)}</a>`}</article>`).join(""):'<div class="tarjeta"><h2>Tutorial en preparación</h2><p class="muted">El administrador todavía no ha publicado materiales.</p></div>';}cargar();